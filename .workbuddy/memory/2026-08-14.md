# 2026-08-14 — yt-dlp-go plan execution completed

Project: `D:\code\yt-dlp-go` — Go rewrite of yt-dlp (foundation + curated feature subset).

## Build/test environment (critical for every Bash call)
- Go 1.26.6 offline at `D:/_gotoolchain/go`. Must put on PATH each call:
  `export PATH=/d/_gotoolchain/go/bin:$PATH`
  plus `export GOCACHE=D:/_gotoolchain/gocache GOPATH=D:/_gotoolchain/gopath GOMODCACHE=D:/_gotoolchain/gopath/pkg/mod`
  (Windows-style absolute paths required or GOPATH errors).
- Module downloads: default `proxy.golang.org` is UNREACHABLE. Use
  `GOPROXY=https://goproxy.cn,direct GOSUMDB=off GOFLAGS=-mod=mod` (goproxy.cn works).
- Network egress is available this session (HTTP 200), so real builds/tests run.

## Plan status (PLAN.md): all 11 phases ✅ as of today
0 baseline/compile · 1 format `-f` engine · 2 output `-o` template · 3 DASH native downloader
· 4 downloader robustness (resume/rate-limit/retry) · 5 postprocessor collection · 6 subtitles
· 7 options/CLI expansion · 8 playlists + concurrent multi-URL (DownloadURLs, error isolation)
· 9 extractors: Bilibili (WBI-signed playurl, pure-Go) + TikTok (og:video/__NEXT_DATA__) + YouTube caption-tracks
· 10 TLS impersonation via utls (build tag `-tags utls`, default stdlib-only)
· 11 final docs (README + CLI help) + gofmt.

## Key design facts
- Extractors self-register via `init()` + `extractor.Register`; core blank-imports them.
- `Info.Entries []*Info` = playlist model; core branches to `downloadPlaylist` (honors
  `--playlist-items`/`--no-playlist`); `DownloadURLs` runs URLs concurrently with error isolation.
- utls is behind `//go:build utls` (transport_utls.go) vs transport_stdlib.go (no-op).
- Tests are offline (httptest + synthetic extractors). Run `go test ./...` (default) and
  `go test -tags utls ./network/` (utls). Both build/vet/test currently green.

## Git
Remote-tracking not pushed; local commits only. Final commit: `0cd24ea`.

## Continuation: Phase 12 — YouTube signature evaluator upgraded to goja (commit `27edb36`)
- User said "继续"; the deferred next step from the plan close-out was upgrading the YouTube
  signature evaluator to goja (was originally requested in the pre-plan message too).
- `extractor/jseval.go`: `DeobfuscateSignature` now PRIMARY-evaluates the player's transform
  function in `github.com/dop251/goja` (pure-Go ES engine); the old regex/classify pipeline is
  kept as a best-effort fallback (only reached if goja errors).
- Extraction machinery: `collectDefs` (brace-matched top-level func/object defs), `extractBlock`
  (string/template/comment-aware brace matcher), `findTransform` (scans source order, keys on
  `split("")...+join("")` pattern + inline reverse/slice/swap OR member-call helper style),
  `referencedNames` (member-call object + call-name expansion), recursive `expand` to embed only
  needed helpers, then `vm.RunString` to define + call the transform.
- `go.mod` now has `go 1.25` and goja as a DIRECT dependency (no `// indirect`); utls remains
  build-tagged. Toolchain 1.26.6 satisfies `go 1.25`.
- New `extractor/jseval_test.go`: object-literal helper (`var V={...}`) form, inlined transform
  form, and no-transform error case — all green.
- README updated: status note no longer "stdlib-only" (goja is a pure-Go dep); module-download
  note covers goja; "What works" YouTube line + Known-limitations #2 reflect goja completion.
- FINAL: default `go build/vet/test ./...` green; `-tags utls` build/vet green; gofmt clean.
- Lesson: yt-dlp-style signature helper methods must RETURN the array (`return a.slice(b)`),
  not just `a=a.slice(b)`, or the caller's reassignment gets `undefined` (broke the first fixture).

