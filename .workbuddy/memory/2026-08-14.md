# 2026-08-14 — yt-dlp-go plan execution completed

Project: `D:\code\yt-dlp-go` — Go rewrite of yt-dlp (foundation + curated feature subset).

## Build/test environment (critical for every Bash call)
- Go 1.26.6 offline at `D:/_gotoolchain/go`. Must put on PATH each call:
  `export PATH=/d/_gotoolchain/go/bin:$PATH`
  plus `export GOCACHE=D:/_gotoolchain/gocache GOPATH=D:/_gotoolchain/gopath GOMODCACHE=D:/_gotoolchain/gopath/pkg/mod`
  (Windows-style absolute paths required or GOPATH errors).
- Module downloads: default `proxy.golang.org` is UNREACHABLE. Use
  `GOPROXY=https://goproxy.cn,direct GOSUMDB=off GOFLAGS=-mod=mod` (goproxy.cn works).
- Network egress is available this session (HTTP 200), so real builds/tests run.

## Plan status (PLAN.md): all 11 phases ✅ as of today
0 baseline/compile · 1 format `-f` engine · 2 output `-o` template · 3 DASH native downloader
· 4 downloader robustness (resume/rate-limit/retry) · 5 postprocessor collection · 6 subtitles
· 7 options/CLI expansion · 8 playlists + concurrent multi-URL (DownloadURLs, error isolation)
· 9 extractors: Bilibili (WBI-signed playurl, pure-Go) + TikTok (og:video/__NEXT_DATA__) + YouTube caption-tracks
· 10 TLS impersonation via utls (build tag `-tags utls`, default stdlib-only)
· 11 final docs (README + CLI help) + gofmt.

## Key design facts
- Extractors self-register via `init()` + `extractor.Register`; core blank-imports them.
- `Info.Entries []*Info` = playlist model; core branches to `downloadPlaylist` (honors
  `--playlist-items`/`--no-playlist`); `DownloadURLs` runs URLs concurrently with error isolation.
- utls is behind `//go:build utls` (transport_utls.go) vs transport_stdlib.go (no-op).
- Tests are offline (httptest + synthetic extractors). Run `go test ./...` (default) and
  `go test -tags utls ./network/` (utls). Both build/vet/test currently green.

## Git
Remote-tracking not pushed; local commits only. Final commit: `0cd24ea`.

## Continuation: Phase 12 — YouTube signature evaluator upgraded to goja (commit `27edb36`)
- User said "继续"; the deferred next step from the plan close-out was upgrading the YouTube
  signature evaluator to goja (was originally requested in the pre-plan message too).
- `extractor/jseval.go`: `DeobfuscateSignature` now PRIMARY-evaluates the player's transform
  function in `github.com/dop251/goja` (pure-Go ES engine); the old regex/classify pipeline is
  kept as a best-effort fallback (only reached if goja errors).
- Extraction machinery: `collectDefs` (brace-matched top-level func/object defs), `extractBlock`
  (string/template/comment-aware brace matcher), `findTransform` (scans source order, keys on
  `split("")...+join("")` pattern + inline reverse/slice/swap OR member-call helper style),
  `referencedNames` (member-call object + call-name expansion), recursive `expand` to embed only
  needed helpers, then `vm.RunString` to define + call the transform.
- `go.mod` now has `go 1.25` and goja as a DIRECT dependency (no `// indirect`); utls remains
  build-tagged. Toolchain 1.26.6 satisfies `go 1.25`.
- New `extractor/jseval_test.go`: object-literal helper (`var V={...}`) form, inlined transform
  form, and no-transform error case — all green.
- README updated: status note no longer "stdlib-only" (goja is a pure-Go dep); module-download
  note covers goja; "What works" YouTube line + Known-limitations #2 reflect goja completion.
- FINAL: default `go build/vet/test ./...` green; `-tags utls` build/vet green; gofmt clean.
- Lesson: yt-dlp-style signature helper methods must RETURN the array (`return a.slice(b)`),
  not just `a=a.slice(b)`, or the caller's reassignment gets `undefined` (broke the first fixture).

## Continuation: Phase 13 — n/nsig deobfuscation via goja + sts injection (commit `a733981`)
- User: "用 goja 继续求值 n/nsig 函数并注入 sts，进一步贴近真实 YouTube。"
- `extractor/jseval.go`: `DeobfuscateSignature(playerJS, sig, sts string)` now takes `sts`
  (player response `sts`) as 2nd arg → goja call `name(sig, sts)`. Added `DeobfuscateNSig(playerJS, n)`
  for the `n` throttling param. Refactored shared eval into `deobfuscateGoja(playerJS, arg, extra, findFn)`
  + `quoteAll`; both sig and nsig reuse it. Added `findNTransform`/`isNTransform`: n-transform heuristic =
  has charAt/substr/substring/.slice AND `return`, but NOT `split("")...+join("")` (that's the sig transform);
  scanned in source order. Sig still keeps regex pipeline as fallback; nsig has no regex fallback (returns error).
- `extractor/youtube/youtube.go`: Extract grabs `player.sts`; `buildFormat` gets `sts` param and calls
  `DeobfuscateSignature(playerJS, sig, sts)`; new `rewriteNParam` parses the format URL, deobfuscates `n`
  via `DeobfuscateNSig`, rewrites it (keeps original URL if JS unavailable / eval fails — graceful). `needsJS`
  now also true when a direct format URL contains `n=` (n-throttling appears on unciphered streams too).
- Tests: `jseval_test.go` NSig goja + no-transform error; `youtube_test.go` sts passthrough (sig(a,b) appends b),
  buildFormat rewrites n (abcdef→fedcba) and leaves URL untouched without JS.
- DEFAULT `go build/vet/test ./...` green; `-tags utls` build/vet green; gofmt clean.
- Caveat (offline): n/sts detection is heuristic + synthetic-tested only; real YouTube player internals
  (exact n-function call site, clientVariables) need live verification, as with all site extractors.

## Continuation: Phase 14 — n transform located by PLAYER CALL SITE (commit `925fa57`)
- User: "继续把 n 函数改为「按播放器调用点精确定位」。"
- Replaced the body-shape scan in `findNTransform` with **call-site localization**:
  `findNByCallSite` uses `reSelfCall` (`x = NAME(x)` self-reassigning single-arg call) and
  `reSelfCallMember` (`x = (0, MOD.NAME)(x)` module form) to find where YouTube APPLIES the n
  transform in place; resolves NAME via `collectDefs` and validates with `isNTransform`. The old
  scan-every-definition heuristic is kept only as `findNByBody` fallback.
- New helpers: `resolveNShaped(defs, name)`, `findMethodInObj(objDef, method)` (brace-matched method
  extraction inside object literals; handles `name:function(...)` and ES6 shorthand `name(...)`).
- Hardened `deobfuscateGoja` `expand`: member-call names like `M.fo` now auto-embed the parent object
  `M` (so `M.fo(n)` resolves). General improvement for both sig helper and nsig module calls.
- Note: RE2 has NO backreferences, so the self-reassign check (lhs == arg) is done in Go code after
  matching `([id])=([id])([id])`, not via a `\1` backref.
- Tests added: `TestDeobfuscateNSig_CallSite` (fixture has a decoy n-shaped function appearing BEFORE
  the real transform but never self-reassigned — proves call-site picks the APPLIED transform, not the
  early decoy) and `TestDeobfuscateNSig_ModuleCallSite` (module form `(0, NMod.throttle)(n)`). Both green.
- DEFAULT `go build/vet/test ./...` green; `-tags utls` build/vet green; gofmt clean.

## Continuation: Phase 15 — Bilibili 端到端可用 + 三处下载 bug 修复 (commit pending)
- User: "现在可以下载bilibili的视频吗？" — 目标：用真实网络验证 Bilibili 提取器能否端到端下载。
- 网络可达性确认：bilibili（`www.bilibili.com`/`api.bilibili.com`）在沙箱可达（HTTP 200）；YouTube 不可达（超时）；无本地代理。因此能真实验证，而非离线 fixture。
- 构建 `ydlgo.exe`，对真实 BV `BV1mJuB6jEDj` 跑 `--simulate` → "船新版本新宝岛！这个联动怎么说？ (7 formats)"；`-f bestaudio` 下载到真实 873KB ISO Media MP4 音频落盘，证明提取链（__INITIAL_STATE__ → WBI 签名 → playurl）全通。
- 验证默认 `-f best` 合并下载时发现并修复三处阻塞 bug：
  1. `format.Select` 的 `best`/`worst` 原按「单一合并格式（V+A 同格式）」选取，Bilibili DASH 是分离流，永远落空 → 改 `selectBestAlias`：`bestvideo+bestaudio` 合并，无分离流时回退合并格式；`format_test.go` 的 `TestSelect_Best`→`[[137 251]]`、`TestSelect_Worst`→`[[92 140]]` 同步更新。
  2. `output.Sanitize` 把 `/` 也替换成 `_`，导致 `-o "_dltest/%(id)s.%(ext)s"` 子目录失效且扩展名被重复拼接（`_dltest_BV1mJuB6jEDj.mp4.m4s`）→ 新增 `output.SanitizePath`（仅 sanitize 各分段、保留分隔符）；`core.outputBase` 去掉模板里已嵌入的 `%(ext)s`。
  3. `postprocessor.Merge` 显式传 `-f mkv`，本机 ffmpeg 8.1.2 拒绝该写法（"Requested output format 'mkv' is not known"）→ 改为交由输出扩展名推断 muxer（`["-y","-i",video,("-i",audio),"-c","copy",output]`）。
- 最终复跑默认合并下载：`_dltest/BV1mJuB6jEDj.mkv`（h264 480x852 + AAC，54.2s，7.1MB），`ffprobe` 校验 stream/format 正常，可播放。
- `go build/vet/test ./...` 全绿（format 测试已更新）；README Known limitations #1 改为「Bilibili 已真实验证可下载（高清需 SESSDATA cookie），YouTube/TikTok 仅结构忠诚+单测、未在沙箱在线验证」。PLAN.md row 15 写毕。
- 待办：本次改动 `git commit`（涉及 core/core.go, format/format.go, format/format_test.go, output/output.go, postprocessor/postprocessor.go, PLAN.md, README.md；`_dltest/`、`extractor/testdata/`、`scripts/` 为新增未跟踪）。
- 经验：ffmpeg 不同构建对 `-f <ext>` 接受度不一；优先用输出扩展名推断 muxer。yt-dlp 的 `best`/`worst` 语义是「最佳视频+最佳音频合并」，不是「单个合并格式」。

